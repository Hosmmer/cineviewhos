from .base import *  # noqa: F401,F403

# Local development settings.
#
# All values are driven by backend/.env through the base settings. Add
# development-only overrides here as they become necessary (e.g. a console
# email backend if you want to avoid sending real email in development).
